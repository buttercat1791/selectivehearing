selectivehearing version 0.9.1 [beta]

Installation:
- Requires Node.js to be installed.
- Download the source code.
- Run npm install in the source code directory to install dependencies.

Usage:
- Run the script with the command node ./lib/src/app.js [number-of-results] [[[query1], query2], ...]
    - number-of-results: Number of results per query
    - queries: Search terms to be used

Output:
- A number of files containing the results with the names query1.csv, query2.csv, ... etc.